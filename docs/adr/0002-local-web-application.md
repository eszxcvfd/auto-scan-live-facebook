# Use a local web application for the user interface

The product will run as a local web application that the user opens in a browser, rather than as a desktop GUI or a hosted multi-user service. This keeps the browser-based interface highly customizable while allowing the discovery process to run locally without introducing hosted deployment and multi-user concerns in the initial product.
