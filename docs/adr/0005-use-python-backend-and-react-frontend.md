# Use a Python backend with a React and TypeScript frontend

LiveScout will use FastAPI for the Python backend and Playwright-based discovery, with React and TypeScript for the local web interface. The frontend will use editable shadcn/ui-style components so the product can have a highly customized interface without giving up a conventional component vocabulary. This balances the Python requirement with the requested web UI flexibility.
